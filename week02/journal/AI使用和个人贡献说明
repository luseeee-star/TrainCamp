1. 我让 AI 帮我做了什么？
我对 CSS 样式以及其它控件方面的细节代码不太熟悉，所以我给ai我想要的效果，ai帮我生成了页面、Vue 3 的基本框架。
关于前后端数据交互与全局拦截器还有token存储我都请教了ai，ai 给了我一些思路和代码片段，我根据这些提示自己动手把它们整合起来了。

2. 我的贡献：
关于apicall方法设计和调用是我写了个毛坯然后ai优化了，但是ai并不知道我后端写的 JSON 格式是什么样的。
我手动修改了代码，让前端能识别出我后端定义的 code: 200 是成功，并且把报错信息 msg 显示在网页上以及一些其它的参数细节。
前端对于数据的合法性判断也是我后期再调整的

Token 存储：
我发现登录完如果不存 Token，一刷新页面就又没登录了。于是我让 AI 引入了 localStorage（浏览器的本地仓库）。我意识到这就像给学生发了个“临时出入证”，存进钱包里，以后出门（发请求）就不用每次都输密码了。

拦截器：
我配置了http.js，学了一下axois的全局拦截机制，让它在每次发请求前自动去localStorage里翻出Token塞进Header。
（虽然最后并没完美运行成功，请教cursor给我改了）
